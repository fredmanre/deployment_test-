from django.apps import AppConfig


class ServicesConfig(AppConfig):
    name = 'services'
    # el verbose name le da una configuracion extendida al modelo(ver settings)
    verbose_name = 'gestor de servicios'
